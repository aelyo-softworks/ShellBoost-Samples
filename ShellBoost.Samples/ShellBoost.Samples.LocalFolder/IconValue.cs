namespace ShellBoost.Samples.LocalFolder
{
    // values here must match Icon property's enumerated values in Resources\LocalFolder.propdesc
    public enum IconValue
    {
        None = 0,
        Ok = 1,
        Error = 2,
        Warning = 3
    }
}
